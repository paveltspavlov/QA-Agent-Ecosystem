pyyaml>=6.0
rich>=13.0

# Provider SDKs — install one or more based on your setup:

# GitHub Copilot (recommended — Agent mode with multi-model access)
# github-copilot-sdk>=0.1.0

# Anthropic Claude (backward compatibility)
# claude-agent-sdk>=0.1.48

# OpenAI / local models (Ollama, LM Studio, vLLM, Together, Groq)
# openai>=1.0
pygments>=2.20.0 # not directly required, pinned by Snyk to avoid a vulnerability
